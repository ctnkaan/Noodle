const { meme, memeAsync } = require('memejs');

// meme(function(err, data) {
//     if (err) return console.error(err);
//     console.log(data);
//   });

  memeAsync('IDontWorkHereLady') // Use memeAsync('subredditname') to filter subreddits
  .then(m => {
    // Do stuff with the JSON
    console.log(m);
  })
  .catch(e => {
    // Handle the error
    console.log(e);
  })